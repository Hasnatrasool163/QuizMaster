/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package tenthListFrames;

/**
 *
 * @author Home
 */
public class dotnet_Quiz {
    
}

//questions.add(" What is the difference between a stack and queue?");
//options.add("A)   Queues process value types by a top-down hierarchy first in first out (FIFO). Stacks follow this principle and insert items from the lower end while deleting ones from the top last in first-out (LIFO)., B)    Queues process value types by a top-down hierarchy-last in first-out (LIFO). Stacks follow this principle and insert items from the lower end while deleting ones from the top-first in first-out (FIFO)., C)   Stacks process value types by a top-down hierarchy last in first-out (LIFO). Queues follow this principle and insert items from the lower end while deleting ones from the top first in first-out (FIFO), D)    Stacks process value types by a top-down hierarchy-first in first out (FIFO). Queues follow this principle and insert items from the lower end while deleting ones from the top-last in first-out (LIFO).");
//answers.add("C)  Stacks process value types by a top-down hierarchy- last in, first-out (LIFO). Queues follow this principle and insert items from the lower end while deleting ones from the top - first in, first-out (FIFO)");
//
//questions.add(" Which group contains all official types of JIT compilations?");
//options.add("A)   Pre-JIT Quick-JIT Coll-JIT, B)    Before-JIT Quick-JIT Cool-JIT, C)    Pre-JIT Quick-JIT Normal-JIT, D)   Pre-JIT Econo-JIT Normal-JIT");
//answers.add("D)  Pre-JIT  Econo-JIT Normal-JIT");
//
//questions.add(" What is Kestrel?");
//options.add("A)   an iOS web server initially that was later designed to work with .NET Core, B)   a cross-platform web server ASP.NET Core that is included by default in ASP.NET Core project templates, C)    a cross-platform open-source web server that starts up web APIs, D)    a free and open-source cross-platform web server software that can communicate with IIS");
//answers.add("B)  a cross-platform web server ASP.NET Core that is included by default in ASP.NET Core project templates");
//
//questions.add(" When would you use asynchronous actions?");
//options.add("A)   to release the quest thread of a I/O operation, B)    to capture the request thread of a I/O operation, C)   to avoid blocking the request thread while waits for an I/O operation, D)    to block the request thread if it waits for an I/O operation");
//answers.add("C)  to avoid blocking the request thread while waits for an I/O operation");
//
//questions.add(" What is CoreCLR?");
//options.add("A)   CoreCLR is a component that allows the highest degree of control when coding; you can Abort() Suspend() or Resume()., B)    CoreCLR is the .NET execution engine that runs the source code. Special programs called compilers must rewrite it into MSIL., C)    CoreCLR is the .NET execution engine the runs the source code. Special programs called compilers must rewrite it into IL., D)   CoreCLR is the .NET execution engine in .NET Core that performs functions like garbage collection and compilation to machine code.");
//answers.add("D)  CoreCLR is the .NET execution engine in .NET Core that performs functions like garbage collection and compilation to machine code.");
//
//questions.add(" When you define an abstract method, how do you use it in a derived class?");
//options.add("A)   Abstract methods cannot be used in derived classes., B)    In your derived class overload the method., C)   In your derived class override the method., D)    In your derived class declare the method as virtual.");
//answers.add("A)  In your derived class, override the method.");
//
//questions.add(" Which code do you use if you want to trigger a garbage collection in .NET?");
//options.add("A)   `Garbage.CleanUp();`, B)    `System.GC.Clear();`, C)   `System.GC.Collect();`, D)    `Garbage.Collect();`");
//answers.add("A)  `System.GC.Collect();`");
//
//questions.add(" You want to include language elements in a program. Which design pattern best fits this objective?");
//options.add("A)  Interpreter <= correct, B)    Command, C)    Bridge, D)    Decorator");
//answers.add("A)  Interpreter <= correct");
//
//questions.add(" What makes a strong-named assembly?");
//options.add("A)   an assembly with the version specified, B)   a signed assembly, C)    an assembly with culture information, D)    an assembly with the name marked as important");
//answers.add("A)  a signed assembly");
//
//questions.add(" What happens when you concatenate two strings?");
//options.add("A)   You cannot concatenate strings in .NET., B)    The second string object is modified so it contains the concatenated strings., C)   A third string object is created containing the concatenated strings., D)    The first string object is modified so it contains the concatenated strings.");
//answers.add("A)  A third string object is created containing the concatenated strings.");
//
//questions.add(" What is a delegate?");
//options.add("A)   A delegate is an object or collection initializer that adds flexibility readability and maintainability in C#., B)   A delegate in .NET is similar to a function pointer in C or C++. Using a delegate allows the programmer to encapsulate a reference to a method inside a delegate object., C)    A delegate is a collection initializer that adds flexibility readability and maintainability in .NET., D)    A delegate is an extension method. Using a delegate allows the programmer to add flexibility readability and maintainability in .NET.");
//answers.add("A)  A delegate in .NET is similar to a function pointer in C or C++. Using a delegate allows the programmer to encapsulate a reference to a method inside a delegate object.");
//
//questions.add(" Which is a set of features that extends the query capabilities of the .NET language syntax by adding sets of new standard query operators that allow data manipulation, regardless of the data source?");
//options.add("A)   XML, B)    C#, C)   LINQ, D)    XAML");
//answers.add("A)  LINQ");
//
//questions.add(" What is the single responsibility principle?");
//options.add("A)   Software entities should be open for extension but closed for modification., B)    Entities must depend on abstractions not on concrete implementations., C)    Objects in a program should be repleaceble with instances of their subtypes without altering the correctness of that programm., D)   A class should have only a single responsibility that is only changes to one part of the software's specification should be able to affect the specification of the class.");
//answers.add("A)  A class should have only a single responsibility- that is, only changes to one part of the software's specification should be able to affect the specification of the class.");
//
//questions.add(" When should a developer use the .NET Standard class library project type?");
//options.add("A)   when you want to increase the .NET API surface area your library can access and allow only .NET Core apps to be compatible with your library, B)   when you want to increase the number of apps that are compatible with your library and decrease the .NET API surface area your library can access, C)    when you want to decrease speed but have more features, D)    when you want to increase compilation speed and have fewer features");
//answers.add("A)  when you want to increase the number of apps that are compatible with your library, and decrease the .NET API surface area your library can access");
//
//questions.add(" What is the difference between a SDK (software development kit) and runtime in .NET Core?");
//options.add("A)   The SDK is the "virtual machine" that hosts and runs the application and abstracts all the interaction with the operating system; the runtime usually includes documentation and other help files., B)   The runtime is the virtual machine that hosts and runs the application and abstracts all the interaction with the operating system; the SDK usually includes documentation and other help files., C)    The runtime compiles code along with the CLR; the SDK usually includes documentation and other help files., D)    The SDK compiles code along with the CLR; the runtime usually includes documentation and other help files.");
//answers.add("A)  The runtime is the virtual machine that hosts and runs the application and abstracts all the interaction with the operating system; the SDK usually includes documentation and other help files.");
//
//questions.add(" What is the Common Type System (CTS)?");
//options.add("A)   the component of CLR in which .NET Framework provides support for several languages since it contains a type system that is common with all the languages, B)   the component of CLI in which .NET Framework provides support for several languages since it contains a type system that is common with all the languages, C)    the component of CLR that allows you to map the content of a file to the logical address of an application, D)    the component of CLR that enables you to run multiple versions of an application or component and CLR on the same computer at the same time");
//answers.add("A)  the component of CLI in which .NET Framework provides support for several languages since it contains a type system that is common with all the languages");
//
//questions.add(" Assuming y is a value type, which is an example of boxing?");
//options.add("A)   y = (int)thisObject;, B)    int y = 3;, C)    y = (int)thisObject=;3;, D)   object thisObject = y;");
//answers.add("A)  object thisObject = y;");
//
//questions.add(" What is an abstract class in .NET?");
//options.add("A)  An abstract class provides a partial implementation for functionality and some abstract or virtual members that must be implemented by the inheriting entities. It can declare fields too., B)    An abstract class allows developers to create new classes that reuse extend and modify the behavior defined in other classes., C)    An abstract class is responsible for keeping track of what is actually executing and where each executing thread is., D)    An abstract class declares a contract or behavior that implementing classes require. It may declare only properties methods and events with no access modifiers. All the declared members must be implemented.");
//answers.add("A)  An abstract class provides a partial implementation for functionality and some abstract or virtual members that must be implemented by the inheriting entities. It can declare fields too.");
//
//questions.add(" What is the namespace for caching information in .NET?");
//options.add("A)   System.Data.Caching;, B)    All.System.Caching;, C)   System.Runtime.Caching;, D)    System.Compiler.Caching;");
//answers.add("A)  System.Runtime.Caching;");
//
//questions.add(" What is an interface in .NET?");
//options.add("A)   An interface provides a partial implementation for functionality and some abstract or virtual members that must be implemented by the inheriting entities. It can declare fields too., B)    An interface is responsible for keeping track of what is actually executing and where each executing thread is., C)    An interface allows developers to create new classes that reuse extend and modify the behavior defined in other classes., D)   An interface declares a contract or behavior that implementing classes require. It may declare only properties methods and events with no access modifiers. All the declared members must be implemented.");
//answers.add("A)  An interface declares a contract or behavior that implementing classes require. It may declare only properties, methods, and events with no access modifiers. All the declared members must be implemented.");
//
//questions.add(" What does CAS stand for and what does it do?");
//options.add("A)   CAS stands for Code Application Secrets and it enables users to restrict on a very granular level what hidden code can do according to a level of trust for an application., B)    CAS stands for Code Access Secrets and it enables users to restrict on a very granular level what hidden code can do according to a level of trust., C)    CAS stands for Cognitive Access Security and it enables users to restrict security logic manipulation., D)   CAS stands for Code Access Security and it enables users to restrict on a very granular level what managed code can do according to a level of trust.");
//answers.add("A)  CAS stands for Code Access Security and it enables users to restrict, on a very granular level, what managed code can do according to a level of trust.");
//
//questions.add(" Which is NOT true about lambda statements?");
//options.add("A)  A statement lambda cannot return a value., B)    If a statement lambda has a return value it has to use a return statement., C)    A statement lambda requires using curly braces., D)    A statement lambda can have more than one statement.");
//answers.add("A)  A statement lambda cannot return a value.");
//
//questions.add(" Which is NOT true about a read-only variable?");
//options.add("A)   At runtime its value is evaluated., B)    It can be either static or an instance member., C)   It can be initialized at declaration only., D)    It can be initialized in either the constructor or the declaration.");
//answers.add("A)  It can be initialized at declaration only.");
//
//questions.add(" When break is used inside two nested for loops, does control come out of the inner for loop or the outer for loop?");
//options.add("A)   It breaks from only the outer loop., B)    It breaks from all loops., C)   It breaks from only the inner loop., D)    It breaks from the outer loop after the second iteration.");
//answers.add("A)  It breaks from only the inner loop.");
//
//questions.add(" You want to separate object construction from its representation. Which design pattern best fits this objective?");
//options.add("A)   Adapter, B)    Bridge, C)    Singleton, D)   Builder");
//answers.add("A)  Builder");
//
//questions.add(" You want to encapsulate a command request as an object. Which design pattern best fits this objective?");
//options.add("A)  Command, B)    Iterator, C)    Facade, D)    Observer");
//answers.add("A)  Command");
//
//questions.add(" Why would Pre-JIT be used by the .NET Framework?");
//options.add("A)   to compile only the methods that are called at runtime and store those methods in cache after execution, B)   to compile complete source code into native code in a single compilation cycle during deployment of the application, C)    to compile only the methods that are called at runtime and then store them in cache for one minute, D)    to compile only the methods that are called at runtime and remove them from memory after execution");
//answers.add("A)  to compile complete source code into native code in a single compilation cycle during deployment of the application");
//
//questions.add(" What do code contracts do?");
//options.add("A)   Code contracts are data structures that can operate on demand per requirements., B)   Code contracts provide a way to specify preconditions postconditions and object invariants in your code., C)    Code contracts contain definitions for a group of related functionalities that a class or a struct can implement., D)    Code contracts contain data structures for a group of related functionalities that a class or a struct can implement.");
//answers.add("A)  Code contracts provide a way to specify preconditions, postconditions, and object invariants in your code.");
//
//questions.add(" You must connect an app to an online identity provider using OAuth. For authentication, the app uses WebAuthenticationBroker object. You need to make sure the app registers with the provider. Which action do you take?");
//options.add("A)   Construct an HTTP request URI and an HTTPS request URI., B)    Invoke the GetCurrentApplicationCallbackUri method and construct an HTTPS request URI., C)    Invoke the AuthenticateAsync and GetCurrentApplicationCallbackUri methods., D)   Invoke the AuthenticateAsync method and construct an HTTPS request URI.");
//answers.add("A)  Invoke the AuthenticateAsync method and construct an HTTPS request URI.");
//
//questions.add(" You want to create a class of which only a single instance can exist. Which design pattern best fits this objective?");
//options.add("A)   Adapter, B)   Singleton, C)    Bridge, D)    Decorator");
//answers.add("A)  Singleton");
//
//questions.add(" Which of the following selects an anonymous type?");
//options.add("A)  select new { a.Country a.Region }, B)    select a, C)    select a.Country a.Region, D)    select { a.Country a.Region }");
//answers.add("A)  select new { a.Country, a.Region }");
//
//questions.add(" Which is NOT true about a constant variable?");
//options.add("A)   At compile time the value is evaluated., B)    Only at declaration can it be initialized., C)   At runtime its value is evaluated., D)    It cannot be static.");
//answers.add("A)  At runtime, its value is evaluated.");
//
//questions.add(" What is the purpose of CLR?");
//options.add("A)   CLR performs various operations such as security checks spell checks assembly loading and thread management. It provides a secure execution environment for the internet., B)    CLR sets the rules developers must use for the components that are interlanguage compatible. CLR is reusable across all the .NET-compliant languages., C)    CLR is a compiler that converts Intermediate Language to a native code., D)   CLR performs various operations such as memory management security checks assembly loading and thread management. It also provides a secure execution environment for applications.");
//answers.add("A)  CLR performs various operations such as memory management, security checks, assembly loading, and thread management. It also provides a secure execution environment for applications.");
//
//questions.add(" What is CIL?");
//options.add("A)  Formerly known as MSIL CIL is a programming language that NET developers use. It represents the lowest possible level for a language that humans can still read., B)    CIL is an object-oriented programming language that is a partially compiled code that .NET developers will then compile to native machine code., C)    CIL is used to convert a value type to an object type., D)    CIL is a compiled code library that Microsoft developed as an open specification. Developers use it for security versioning and deployment purposes.");
//answers.add("A)  Formerly known as MSIL, CIL is a programming language that NET developers use. It represents the lowest possible level for a language that humans can still read.");
//
//questions.add(" **\_** pattern works as a bridge between two incompatible interfaces? // wording in question is maybe changed?");
//options.add("A)  Adapter, B)    Bridge, C)    Singleton, D)    Builder");
//answers.add("A)  Adapter");
//
//questions.add(" Why would you use ahead-of-time (AOT) compilation?");
//options.add("A)  You can deliver a faster startup time especially in big applications where much code executes on startup., B)    You have a slow processor., C)    You have little memory and disk space, D)    The JIT compiler does not have to do a lot of disk I/O actions which are quite expensive.");
//answers.add("A)  You can deliver a faster startup time, especially in big applications where much code executes on startup.");
//
//questions.add(" Which statement describes a Dispose method?");
//options.add("A)   It defines an execution environment for program code and manages the CLR, B)   It belongs to the IDisposable interface and is used to free resources such as network connection and files., C)    It is partially method-compiled code library for use in deployment versioning and security., D)    It is used for encapsulation of collections tied to functions of a class and object.");
//answers.add("A)  It belongs to the IDisposable interface and is used to free resources, such as network connection and files.");
//
//questions.add(" What is a thread?");
//options.add("A)   A single operation that does not return a value and that usually executes asynchronously, B)   the basic unit to which an operating system allocate processor time, C)    a series of related tasks or methods that together turn inputs into outputs, D)    a program that is running on your computer");
//answers.add("A)  the basic unit to which an operating system allocate processor time");
//
//questions.add(" You want to add responsibilities to object dynamically. Which design pattern best fit this objective?");
//options.add("A)  Decorator, B)    Bridge, C)    Singleton, D)    Facade");
//answers.add("A)  Decorator");
//
//questions.add(" Which choice creates an 8-tuple containing prime numbers that are less than 20?");
//options.add("A)  `var primes = Tuple.Create(2 3 5 7 11 13 17 19);`, B)    `var primes = Tuple.Create(2 3 5 7 11 13 17 21);`, C)    `var primes = Tuple.Make(2 3 5 7 11 13 17 20);`, D)    `var primes = Tuple.Make(2 3 5 7 11 13 17 19);`");
//answers.add("A)  `var primes = Tuple.Create(2, 3, 5, 7, 11, 13, 17, 19);`");
//
//questions.add(" Where should you store connection string information?");
//options.add("A)   in any file within the namespace of the program, B)    in the view, C)   in configuration files, D)    in the database");
//answers.add("A)  in configuration files");
//
//questions.add(" Why use design patterns?");
//options.add("A)   design patterns make the code more efficient with memory usage, B)    design patterns minimize the number of code lines when creating complex applications, C)    design patterns tend to be more secure and prevent code from being hacked, D)   design patterns help you solve issues related to sofware development using a proven solution and make communication between developers more efficient");
//answers.add("A)  design patterns help you solve issues related to sofware development using a proven solution, and make communication between developers more efficient");
//
//questions.add(" What is a task?");
//options.add("A)   the basic unit to which an operating system allocates processor time, B)    a program that is running on your compiler, C)   a single operation that does not return a value and that usually executes asynchronously, D)    a series of related methods that together turn inputs into outputs");
//answers.add("A)  a single operation that does not return a value and that usually executes asynchronously");
//
//questions.add(" Which choice is NOT a component of .NET Framework?");
//options.add("A)   common language JIT, B)   .NET framework class library, C)    common language runtime, D)    side-by-side execution");
//answers.add("A)  .NET framework class library");
//
//questions.add(" Which statement about the `this` keyword is _not_ true?");
//options.add("A)   The `this` keyword lets a constructor call a different constructor in the same class., B)   A constructor can use a base statement and a `this` statement if the base statement comes first., C)    A constructor can use one `this` statement at most., D)    If a constructor uses a `this` statement its code is executed after the invoked constructor is executed.");
//answers.add("A)  A constructor can use a base statement and a `this` statement if the base statement comes first.");
//
//questions.add(" When should you use the .NET Core class library project type?");
//options.add("A)  when you want to increase the .NET API surface area your library can access and allow only .NET Core apps to be compatible with your library, B)    when you want to increase the number of apps that are compatible with your library and decrease the .NET API surface area your library can access, C)    when you want to decrease speed but have more features, D)    when you want to increase compilation speed and have fewer features");
//answers.add("A)  when you want to increase the .NET API surface area your library can access, and allow only .NET Core apps to be compatible with your library");
//
//questions.add(" What is .NET?");
//options.add("A)   .NET is a general-purpose programming language. The language has expanded significantly over time and now has object-oriented generic and functional features in addition to facilities for low-level memory manipulation., B)    .NET is a virtual machine that enables a computer to run programs written in several languages and compile programs to bytecode., C)    .NET is an interpreted high-level general-purpose programming language. Its language constructs an object-oriented approach aimed at helping programmers write clear logical code for small and large-scale projects., D)   .NET is a free cross-platform open-source developer platform for building many different types of applications with multiple languages editors and libraries for web mobile desktop gaming and IoT.");
//answers.add("A)  .NET is a free, cross-platform, open-source developer platform for building many different types of applications with multiple languages, editors, and libraries for web, mobile, desktop, gaming, and IoT.");
//
//questions.add(" The ASP.NET Core Module is a native IIS module that plugs into the IIS pipeline to either **\_**.");
//options.add("A)   host an ASP.NET Code app inside of the IIS worker process called the out-of-process hosting model or forward web requests to a backend ASP.NET Core app running the Kesrel server called the in-process hosting model, B)    create IIS code the server needs in a file with the extension required or run the IIS server in a mode compatible for ASP.NET Core, C)    package up your C# application and C# packages into .NET modules or specify which of its packages should be visible to other .NET modules, D)   host an ASP.NET Code app inside of the IIS workes process called the in-process hosting model or forward web requests to a backend ASP.NET Core app running the Kestrel server called the out-of-process hosting model");
//answers.add("A)  host an ASP.NET Code app inside of the IIS workes process, called the in-process hosting model, or forward web requests to a backend ASP.NET Core app running the Kestrel server, called the out-of-process hosting model");
//
//questions.add(" What method do you use to explicitly kill a user's session?");
//options.add("A)   `Session.Timeout()`, B)   `Session.Abandon()`, C)    `Session.KillAll()`, D)    `Session.Remove()`");
//answers.add("A)  `Session.Abandon()`");
//
//questions.add(" Which choice best describes the difference between globalization and localization?");
//options.add("A)   Localization enables the .NET runtime to provide services to managed code so language compilers can emit metadata and references in your code. Globalization reverses this process., B)    Globalization enables the .NET runtime to provide services to managed code so language compilers can emit metadata and references in your code. Localization reverses this process., C)    Localization involves designing and developing a world-ready app that supports localized interfaces and regional data for users in multiple cultures. Globalization is the process of translating an application's resources into versions for each culture that the application will support., D)   Globalization involves designing and developing a world-ready app that supports localized interfaces and regional data for users in multiple cultures. Localization is the process of translating an application's resources into versions for each culture that the application will support.");
//answers.add("A)  Globalization involves designing and developing a world-ready app that supports localized interfaces and regional data for users in multiple cultures. Localization is the process of translating an application's resources into versions for each culture that the application will support.");
//
//questions.add(" What does CIL stand for?");
//options.add("A)   C# Interpreted Language, B)   Common Intermediate Language, C)    Commonly Interpreted Language, D)    C# Intermediate Language");
//answers.add("A)  Common Intermediate Language");
//
//questions.add(" Which choice best describes the difference between a namespace and an assembly?");
//options.add("A)   Namespace contains code to form MSIL (Microsoft Intermediate Language). An assembly contains a set of unique names., B)   Namespace is the logical naming decided at design time by the developer. Scope for a particular type is defined at run time using an assembly., C)    A namespace has logical units that are physically grouped together. An assembly does not have any classification., D)    A namespace has logical units that are physically grouped together for assemblies. Assembly classes are available in your program and will be logically grouped.");
//answers.add("A)  Namespace is the logical naming decided at design time by the developer. Scope for a particular type is defined at run time using an assembly.");
//
//questions.add(" What is a tuple?");
//options.add("A)  A data structure that has a specific number and sequence of elements, B)    An object to store multiple variables of the same type in an array data structure, C)    A group of classes designed specifically for grouping together objects and performing tasks on them, D)    An array whose elements are jagged");
//answers.add("A)  A data structure that has a specific number and sequence of elements");
//
//questions.add(" What does IL stand for?");
//options.add("A)  Intermediate Language, B)    Intelligent Language, C)    Intelligent Linq, D)    Interpreted Language");
//answers.add("A)  Intermediate Language");
//
//questions.add(" You want to create an instance of several families of classes. Which design pattern best fits this objective?");
//options.add("A)   Decorator, B)    Singleton, C)   Abstract Factory, D)    Bridge");
//answers.add("A)  Abstract Factory");
//
//questions.add(" Which statement about a read-only variable is _not_ true?");
//options.add("A)   It can be either static or an instance memeber., B)    At run time its value is evaluated., C)    It can be initialized in either the constructor or the declaration., D)   It can be initialized at declaration only.");
//answers.add("A)  It can be initialized at declaration only.");
//
//questions.add(" What is the interface segregation principle?");
//options.add("A)  Many client-specific interfaces are better than one general-purpose interface., B)    Software entities should be open for extension but closed for modification., C)    A class should have only a single responsibility that is only changes to one part of the software's specification should be able to affect the specification of the class., D)    Objects in a program should be replaceable with instances of their subtypes without altering the correctness of that program.");
//answers.add("A)  Many client-specific interfaces are better than one general-purpose interface.");
//
//questions.add(" Why would the .NET Framework use Econo-JIT (Just-inTime)?");
//options.add("A)   to compile only the methods that are called at run time and then store them in cache for one minute, B)    to compile only methods called at run time which are compiled the first time the methods are called and then stored in cache to be used for execution when the same methods are called again, C)    to compile complete source code into native code in a single compilation cycle during deployment of the application, D)   to compile only the methods that are called at run time and remove them from memory after execution");
//answers.add("A)  to compile only the methods that are called at run time and remove them from memory after execution");
//
//questions.add(" What is the difference between a heap and a stack?");
//options.add("A)  The stack contains stored value types; the heap contains stored reference types., B)    The heap is stored value types; the stack is stored reference types., C)    The heap is stored object types; the stack is stored class types., D)    The stack is stored object types; the heap is stored class types.");
//answers.add("A)  The stack contains stored value types; the heap contains stored reference types.");
//
//questions.add(" What is open closed principle?");
//options.add("A)  software entities should be open for extension but closed for modification., B) , C)  , D) ");
//answers.add("A)  software entities should be open for extension, but closed for modification.");
//
//questions.add(" How do you make sure that the garbage collector is done running when you call `GC.Collect()`?");
//options.add("A)   There is no way to find this out., B)    You cannot. [`GC.Collect()`](https://docs.microsoft.com/en-us/dotnet/api/system.gc.collect) is a blocking method and will return only once the garbage collector is done running., C)   by calling [`GC.WaitForFullGCComplete()`](https://docs.microsoft.com/en-us/dotnet/api/system.gc.waitforfullgccomplete), D)    by calling [`GC.WaitForPendingFinalizers()`](https://docs.microsoft.com/en-us/dotnet/api/system.gc.waitforpendingfinalizers)");
//answers.add("A)  by calling [`GC.WaitForFullGCComplete()`](https://docs.microsoft.com/en-us/dotnet/api/system.gc.waitforfullgccomplete)");
//
//questions.add(" What does JIT do in .Net?");
//options.add("A)   JIT is used for deployment versioning and security., B)   JIT translates the IL code to an assembly code and uses the CPU architecture of the target machine to execute a .NET application., C)    JIT compiles to C# code using .NET environment properties., D)    JIT is used for deployment versioning and security and forces the computer's processor to execute the native machine code.");
//answers.add("A)  JIT translates the IL code to an assembly code and uses the CPU architecture of the target machine to execute a .NET application.");
//
//questions.add(" Which statement about a constant variable is not `true`?");
//options.add("A)  At run time its value is evaluated., B)    It cannot be static., C)    Only at declaration can it be initialized., D)    At compile time the value is evaluated.");
//answers.add("A)  At run time, its value is evaluated.");
//
//questions.add(" What is the difference between managed and unmanaged code?");
//options.add("A)  Managed code is code that is handled by the common language runtime (CLR). Unmanaged code is any code that does not depend on CLR for execution., B)    Managed code is any code that does not depend on the common language runtime (CLR) for execution. Unmanaged code is code that is handled by the CLR., C)    Managed code is code that is handled by Microsoft Intermediate Language (MSIL). Unmanaged code is any code that does not depend on MSIL for execution., D)    Managed code is any code that does not depend on Microsoft Intermediate Language (MSIL) for execution. Unmanaged code is code that is handled by MSIL.");
//answers.add("A)  Managed code is code that is handled by the common language runtime (CLR). Unmanaged code is any code that does not depend on CLR for execution.");
//
//questions.add(" Why use the Configure method of startup class?");
//options.add("A)   It can be used to configure the application services., B)   It can ensure that middleware runs before or after middleware added by libraries at the start or end of the app's request processing pipeline., C)    It can specify how the app respondes to HTTP requests., D)    It can specify how the app makes requests to HTTP.");
//answers.add("A)  It can ensure that middleware runs before or after middleware added by libraries at the start or end of the app's request processing pipeline.");
//
//questions.add(" You want to match interfaces of different classes. which design pattern best fits this objective?");
//options.add("A)   Decorator, B)    Bridge, C)    Singleton, D)   Adapter");
//answers.add("A)  Adapter");
//
//questions.add(" What is IL?");
//options.add("A)  an object-oriented programming language that is a partially compiled code that .NET developers will then compile to native machine code, B)    a programming language methodology that enables developers to change data using a succinct yet expressive syntax by instilling Microsoft .NET-based languages with the capability to make queries, C)    a programming language that .NET developers use and that represents the lowest possible level for a language that humans can still read, D)    a programming language that .NET developers cannot use and that represents the lowest possible level for a language that humans cannot read");
//answers.add("A)  an object-oriented programming language that is a partially compiled code that .NET developers will then compile to native machine code");
//
//questions.add(" What is the difference between covariance and contravariance?");
//options.add("A)   In C# covariance and contravariance disable implicit reference conversion for array types delegate types and generic type arguments. Contravariance preserves assignment compatibility and covariance reverses it., B)   In C# covariance and contravariance enable implicit reference conversion for array types delegate types and generic type arguments. Covariance preserves assignment compatibility and contravariance reverses it., C)    In C# covariance and contravariance enable implicit reference conversion for array types delegate types and generic type arguments. Contravariance preserves assignment compatibility and covariance reverses itIn C# covariance and contravariance disable implicit reference conversion for array types delegate types and generic type arguments. Contravariance preserves assignment compatibility and covariance reverses it., D)    In C# covariance and contravariance disables implicit reference conversion for array types delegate types and generic type arguments. Covariance preserves assignment compatibility and contravariance reverses it.");
//answers.add("A)  In C#, covariance and contravariance enable implicit reference conversion for array types, delegate types, and generic type arguments. Covariance preserves assignment compatibility and contravariance reverses it.");
//
//questions.add("77: Which choice is _not_ part of an assembly?");
//options.add("A)   manifest, B)    CIL, C)    MSIL, D)   resources");
//answers.add("A)  resources");

